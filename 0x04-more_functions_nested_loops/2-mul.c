#include "main.h"
#include <stdio.h>

/**
 *  mul - a function that multiplies two integers
 * @a: first integers
 * @b: second integers
 * Return: multiplication result of the 2 integers
 */
int mul(int a, int b)
{
	return (a * b);
}

